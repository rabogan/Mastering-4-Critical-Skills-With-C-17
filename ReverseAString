#include <iostream>
using namespace std;

string reverse_str(string const& str)
{
	string reverse = "";

	for (int i = 0; i < (int)str.size(); ++i)
	{
		reverse += str[str.size() - i - 1];
	}
	return reverse;
}

int main()
{
	string input;
	cin >> input;

	cout << reverse_str(input);
}
